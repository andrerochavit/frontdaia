# ─── Effectuation AI Backend ────────────────────────────────
# Em produção, aponte para a URL do servidor Flask.
# Em dev, remova esta variável para usar o proxy "/api" → localhost:8001
# ou defina explicitamente http://localhost:8001 para falar direto com o backend.
VITE_EFFECTUATION_API_URL=http://localhost:8001
VITE_EFFECTUATION_API_TOKEN=dev-internal-token
